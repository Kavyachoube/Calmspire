@model IEnumerable<CalmSpire.Models.Assessment>

<h2>Available Assessments</h2>
<ul>
    @foreach (var a in Model)
    {
        <li>
            <b>@a.Title</b> - @a.Description
            <a href="@Url.Action("Start","Assessment", new { id=a.Id })">Start</a>
        </li>
    }
</ul>
